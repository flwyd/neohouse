<div class="card mb-3">
  {{#if title}}
  <div class="card-header {{if type (concat 'bg-' type)}}">{{if icon (fa-icon icon size='lg')}} {{title}}</div>
  {{/if}}
  <div class="card-body">
    {{yield}}
  </div>
</div>
